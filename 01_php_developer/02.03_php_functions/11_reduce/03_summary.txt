У меня в решении использована только функция reduce, но -

это не совсем правильно с точки зрения семантики данных функций. array_reduce() следует использовать для агрегации данных, array_filter, как следует из названия, используется для фильтрации коллекций и наконец array_map() для преобразования элементов.